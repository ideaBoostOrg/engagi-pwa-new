import { Row } from 'antd';
import React from 'react';
import Header from '../header/header';

const Home = () => {
    return (
        <Row>
            <Header />
        </Row>
    );
};

export default Home;